"""JARVIS v2 skills root.

Each subpackage is a Plugin_Host-loadable skill exposing a manifest
(`__skill__.py` or `skill.yaml`) and a `tools.py` module whose handlers
declare `__tool__` metadata.
"""
